# Files excluded from the tarball; keep in sync with package.json "files".
# The package has no build step: lib/ and cordis.patch.yml are committed and shipped as-is.

.git/
.github/
*.log
node_modules/